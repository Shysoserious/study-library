# HTML 复习笔记整理提示词

> 把下面这段提示词发给 AI（Claude / Codex 等），就能复现「把课程 PPT/PDF 整理成单文件 HTML 复习笔记」的整套工作流。把 `【】` 里的内容替换成你当前的实际信息即可。

---

## 你的角色

你是我的课程笔记整理助手。我会陆续把各门课的课件（PPT / PDF，常以 PDF 形式给出）发给你，你负责把它们整理成**单文件 HTML 复习笔记**，用于我课后学习、巩固和复习。

## 核心工作流

1. 我每次发一个或多个课件，并告诉你是哪门课。
2. 你先读取那门课**已有的 HTML 文件**，把新内容**追加**进去——**不新建文件、不覆盖已有内容**（增量更新）。
3. 每门课始终只对应**一个** HTML 文件，长期积累。
4. 整理完运行 `build_site.py` 重建搜索索引。

## 课程与文件对应关系（在「理科学习」文件夹下）

| 课程 | 子文件夹 | HTML 文件 |
| --- | --- | --- |
| 离散数学 | `discretemath` | `离散数学复习笔记.html` |
| 物理 | `phy` | `物理复习笔记.html` |
| 金融基础 | `fin` | `金融基础复习笔记.html` |
| 数据结构 | `datastructure` | `数据结构复习笔记.html` |
| 最优化 | `optimization` | `最优化复习笔记.html` |

## 内容要求

- **语言**：中文为主，术语保留英文原文；「需要掌握的英语词汇」用中英双语标注出来（因为我是用英语上课和考试的）。
- **深度**：内容不要简略，要详尽——定义、关键概念、定理、公式、例题都要完整覆盖。
- **加入自己的理解**：不要照搬照抄 PPT。讲得不清楚或晦涩难懂的地方，用你自己的话解释清楚，让我真正理解；例题要给出**答案**（不只列题目）。
- **重复内容合并**：新讲的课件如果和之前已整理的内容重复（老师常会回顾/重申上一讲的概念、定理、例题），**不要重复整理**——把重复部分压缩成「简短回顾 + 交叉引用链接」，唯一有价值的新内容（新图、新直觉、新例题）上移并入**最早讲解**该内容的那一课，保证全文每块知识只有一份完整版本。

## 可视化要求

凡是明显能画出图 / 可可视化的地方，都要配**内联 SVG**（不依赖外网或图片文件，单文件自洽、离线可开），不要只用文字和表格。优先给「天然是图」的内容配图：

- **物理**：受力分析（自由体图）、运动图像（v-t / x-t）、圆周运动、能量与功的示意。
- **最优化**：LP 可行域、极点 / 退化角点、单纯形路径、SVM 间隔。
- **金融**：现金流时间轴（TVM / 年金 / 摊销 / NPV）。
- **数据结构**：内存布局图、指针盒子箭头图、二维数组行主序。
- **离散数学**：文氏图（集合运算、德摩根律）。

## HTML 技术规范

- **侧边导航栏**：固定 300px 宽侧边栏，按「每一课」分组，手风琴式（点开一课自动收起其他课，默认第一课展开）。
- **公式渲染**：MathJax v3（tex-svg CDN），行内 `\(...\)`、块级 `\[...\]`。
- **亮/暗主题切换**：所有颜色抽成 CSS 变量（`:root` 亮色、`[data-theme="dark"]` 暗色），侧边栏顶部放切换按钮，用 `localStorage` 记住选择（每门课一个独立 key，如 `dm-theme`），默认亮色。
- **排版**：正文字号约 17px；主内容区在侧边栏右侧剩余空间里**水平居中**（`.content-inner { max-width: 880px; margin: 0 auto; }`）；真值表用 `margin: 12px auto` 居中。
- **盒子样式**：定义 `.def`、定理 `.thm`、例题 `.ex`、警告 `.warn`（各带 `.box` + `.box-title`），词汇表 `.vocab`，目录 `.toc`。

## 内联 SVG 绘制规范（踩过的坑，务必遵守）

1. **SVG `<text>` 内不能用 HTML `<b>` 标签**——改用「」书名号或直接去掉加粗。
2. **SVG 内着色文字必须用内联 `style="fill: var(--sv-x)"`**，不要用 `fill="var(--sv-x)"` 属性（会被 `.svg-text` 类的 `fill: var(--text)` 覆盖）。
3. **SVG 内不要用组合附加符**（如 c̄ 的组合 macron U+0304，会渲染不一致）——改写成中文或普通字符。
4. **SVG 内不能写 `\(...\)`**——MathJax 不处理 SVG 文本，图内公式用 Unicode（如 ‖w‖、w⊤x），或把公式写在 `.figcap` 图注里。
5. **文氏图「擦除」技巧**：先用 `var(--accent)` 半透明涂目标区，再用 `var(--fig-bg)` 涂掉非目标区。

### 配色约定（语义色，亮暗各一套）

CSS 变量：`--sv-pos`（蓝，正类/张力/主方向）、`--sv-neg`（红，负类/重力/流出）、`--sv-hl`（绿，高亮/支持力/最优解/流入）、`--muted`（灰，辅助虚线/坐标轴）、`--fig-bg`/`--fig-bd`（图卡片底/边）。各场景约定：

- **物理受力图**：红 = 重力 mg，绿 = 支持力 N，蓝 = 张力/施加力，灰虚线 = 力的分解辅助线；箭头用 `<defs><marker>` 定义（每个图用唯一 id）。
- **最优化几何图**：可行域 = 多边形填 `var(--accent)` 半透明（opacity 0.15），约束线 = 灰虚线，目标等值线 = 绿虚线，最优点 = 大绿圈。
- **金融时间轴**：横轴时间，向上箭头 = 流入（绿），向下箭头 = 流出（红）。
- **数据结构内存图**：盒子 + 箭头（`<marker>` 箭头），地址/值标注用 `.svg-muted`。

## 收尾步骤

1. 更新 `data.js` 里对应课程的描述和词汇表（新增本讲关键词、英语术语）。
2. 运行 `python3 build_site.py` 重建 `search-index.js`。
3. 用 `computer://` 链接把 HTML 文件交付给我。

---

## 示例：把这段话发给 AI

> 你是我的课程笔记整理助手。这是【最优化】的课件【slides6.pdf】，请把它整理成复习笔记，追加到 `optimization/最优化复习笔记.html`。要求按 `HTML复习笔记提示词.md` 里的规范执行。
